// ES6 string template can be used to display variable values inside a string. It
// also supports multiline string without the need to write \n character.
const firstName = "Khairul";
const lastName = "Islam";
console.log(`My name is ${firstName} ${lastName}.`);

const info = `I'm from Narayanganj,
Dhaka,
Bangladesh.`;
console.log(info);